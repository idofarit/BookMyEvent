import { Belanosima, Lobster, Raleway } from "next/font/google";

export const lobster = Lobster({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const belanosima = Belanosima({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const raleway = Raleway({
  subsets: ["latin"],
  display: "swap",
  weight: "300",
});
